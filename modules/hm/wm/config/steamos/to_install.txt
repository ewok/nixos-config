sudo steamos-readonly disable

# setting up pacman
sudo pacman-key --init
sudo pacman-key --populate archlinux
sudo pacman-key --populate
sudo pacman -Syu base-devel git paru

# rm -rf yay-bin && git clone https://aur.archlinux.org/yay-bin.git && cd yay-bin && makepkg -si

paru -S --noconfirm \
qt5-virtualkeyboard \
ttf-firacode-nerd \
i3-wm \
wmctrl \
base-devel \
gameconqueror \
tailscale \
feh xclip func-e-bin xorg-xkill \
awesome-terminal-fonts wezterm

set upt KDW i3 with systemd
